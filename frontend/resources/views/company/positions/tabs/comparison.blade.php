@php($rows = $positionDetail['comparison'] ?? [])
<section class="panel-card p-6">
    <h2 class="text-lg font-semibold">{{ __('company_positions.tabs.comparison') }}</h2>
    <p class="panel-muted mt-1 text-sm">Adaylar yalnız aynı etkin skor sürümüne göre yan yana karşılaştırılır.</p>
    @if($rows === [])<div class="mt-6 rounded-xl border border-dashed border-slate-300 p-8 text-center dark:border-slate-700"><p class="panel-muted">Karşılaştırılabilir aday henüz yok.</p></div>@else<div class="mt-6 grid gap-5 lg:grid-cols-2">@foreach($rows as $candidate)<article class="rounded-2xl border border-slate-200 p-5 dark:border-slate-800"><div class="flex justify-between gap-4"><h3 class="font-semibold">{{ $candidate['candidate_name'] }}</h3><strong class="company-accent-text">{{ $candidate['score'] ?? '—' }}</strong></div><h4 class="mt-4 text-sm font-semibold">{{ __('company_positions.sections.evidence') }}</h4><ul class="panel-muted mt-2 space-y-1 text-sm">@foreach(($candidate['evidence'] ?? []) as $item)<li>{{ is_array($item) ? ($item['evidence'] ?? $item['text'] ?? json_encode($item, JSON_UNESCAPED_UNICODE)) : $item }}</li>@endforeach</ul><h4 class="mt-4 text-sm font-semibold">{{ __('company_positions.sections.uncertainties') }}</h4><ul class="panel-muted mt-2 space-y-1 text-sm">@foreach(($candidate['uncertainties'] ?? []) as $item)<li>{{ is_array($item) ? ($item['message'] ?? json_encode($item, JSON_UNESCAPED_UNICODE)) : $item }}</li>@endforeach</ul></article>@endforeach</div>@endif
</section>
